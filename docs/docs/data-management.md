# Data management

JLib Storage is intended for simple in-memory data management within an application or service.

The storage classes maintain their values for the lifetime of the store instance. They do not provide persistence or database integration.

For more advanced storage requirements, applications should use an appropriate persistent storage solution rather than relying on these in-memory components.